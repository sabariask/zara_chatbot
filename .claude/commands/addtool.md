Add a new tool to chatbot.py called $ARGUMENTS.

Follow these steps exactly:
1. Create the Python function with proper docstring and type hints
2. Add the tool definition to tool_definitions list with clear description
3. Add the case to run_tool() dispatcher
4. Return structured JSON with success field

Follow all standards in CLAUDE.md.
Show me the three changes you made and confirm they are in sync.